// Example of a vanilla JavaScript application entry point

// Import necessary modules or dependencies
import { App } from './app';

// Initialize your application
document.addEventListener('DOMContentLoaded', () => {
  const app = new App();
  app.start();
});
